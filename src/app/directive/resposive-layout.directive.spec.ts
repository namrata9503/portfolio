import { ResposiveLayoutDirective } from './resposive-layout.directive';

describe('ResposiveLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new ResposiveLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
